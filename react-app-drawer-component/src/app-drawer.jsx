import React from 'react';

/*class AppDrawer extends React.Component {
  constructor(props) {
    super(props);
    this.handleClick = this.handleClick.bind(this);
    this.menu = {
      showIcon: "fas fa-bars fa-2x",
      hideIcon: "fas fa-bars fa-2x hidden",
      menuShow: "gray",
      menuHide: "gray hidden"
    }
    this.state = { isHidden: false }
  }
  handleClick() {
    this.setState({isHidden: !this.state.isHidden})
  }
  render () {
    return (
    <div>
      <i onClick={this.handleClick} className={this.state.isHidden ? this.menu.hideIcon : this.menu.showIcon}></i>
        <div onClick={this.handleClick} className={this.state.isHidden ? this.menu.menuShow : this.menu.menuHide}>
          <div className="menu">
          <div className="menu-list">
            <h1>Menu</h1>
            <ul>
                <li onClick={this.handleClick}>About</li>
                <li onClick={this.handleClick}>Get Started</li>
                <li onClick={this.handleClick} >Sign In</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
    )
  }
}

export default AppDrawer; */
